# AOSP13 迁移流程

本文档根据当前目录下的 `fix.py`、`start_repo.sh`、`sync.sh`、`tspi_1f_rk3566_linux.xml`、`final_manifest.xml` 整理，描述这套脚本实际落地的迁移流程。

## 1. 迁移目标

本流程的目标是：

1. 以 `tspi_1f_rk3566_linux.xml` 为原始输入清单。
2. 将原始 manifest 中的项目名、分支和远端地址转换成适合私有 Gitea 的格式。
3. 将源码根目录下的各子项目拆分并推送到 `http://10.161.60.141:3000/aosp13/` 组织下。
4. 在新的工作区中通过 `repo init` + `repo sync` 从私有 Gitea 重新拉取，验证迁移结果。

当前脚本处理的数据规模为：

1. 原始 manifest 共 `1168` 个 `project`。
2. 默认 remote 为 `aosp`，额外使用 `rk` 和 `rk29`。
3. 原始 manifest 中有 `38` 个项目未显式写 `path`，会在转换时自动补齐。

## 2. 文件职责

| 文件 | 作用 |
| --- | --- |
| `tspi_1f_rk3566_linux.xml` | 原始 manifest，项目名包含 `/`，`revision` 多为 commit 或 tag，remote 的 `fetch` 为 `.`，并带有 `review` 属性。 |
| `fix.py` | 清洗原始 manifest，生成可用于私有仓库同步的 `final_manifest.xml`。 |
| `final_manifest.xml` | 转换后的 manifest，project 名已改成下划线风格，`revision` 已统一为 `aosp13`，remote `fetch` 已改为 Gitea 地址。 |
| `start_repo.sh` | 在源码根目录扫描项目路径、检查子仓状态，并批量创建 Gitea 仓库后执行强制推送。 |
| `sync.sh` | 在新的工作区使用私有 manifest 仓库重新 `repo init` / `repo sync`。 |
| `repo` | 本地 `repo` 工具入口，`sync.sh` 会先用它初始化，再切换到 `./.repo/repo/repo` 执行同步。 |

## 3. 脚本中的关键转换规则

`fix.py` 实际做了以下转换：

1. 所有 `<remote>` 的 `fetch` 统一改为 `http://10.161.60.141:3000/aosp13/`。
2. 所有 `<remote>` 的 `review` 属性被删除。
3. `<default>` 的 `revision` 统一改成 `aosp13`。
4. 每个 `<project>` 的 `name` 都会把 `/` 替换为 `_`。
5. 每个 `<project>` 的 `revision` 都会强制改成 `aosp13`。
6. 每个 `<project>` 保留已有 `path`；如果没写 `path`，则自动用原始 `name` 作为 `path`。
7. 每个 `<project>` 会删除 `upstream`、`dest-branch`、`clone-depth`。

可以直接理解成下面这种映射：

```text
原始项目名: device/amlogic/yukawa
转换后仓库名: device_amlogic_yukawa
工作区路径: device/amlogic/yukawa
目标分支: aosp13
```

这也是为什么 `start_repo.sh` 扫描时会：

1. 用 `name` 生成 Gitea 仓库名时把 `/` 替换成 `_`。
2. 用 `path` 或原始 `name` 作为本地目录路径。

这样推送出来的仓库名，才能和 `final_manifest.xml` 中的 `<project name="...">` 对得上。

## 4. 前置条件

执行前需要满足以下条件：

1. 已有一份完整的 AOSP 源码工作区，目录结构与原始 manifest 对应。
2. 当前用户对 Gitea `aosp13` 组织有创建仓库和推送代码权限。
3. `start_repo.sh` 中的以下参数已经按实际环境改好：

```bash
GITEA_HOST="10.161.60.141:3000"
GITEA_ORG="aosp13"
GIT_USER="liuxin"
GIT_PASS=""
TARGET_BRANCH="aosp13"
XML_FILE="tspi_1f_rk3566_linux.xml"
JSON_OUTPUT="project_map.json"
```

4. `fix.py` 中的 `NEW_FETCH_URL` 已指向实际私有仓库根地址。
5. 目标机具备 `bash`、`git`、`curl`、`python3`。
6. 如果要执行 `sync.sh`，远端 `aosp13/manifests` 仓库里必须已经存在 `final_manifest.xml`，且位于 `master` 分支，因为脚本里写死了：

```bash
./repo init \
  -u http://10.161.60.141:3000/aosp13/manifests \
  -b master \
  -m final_manifest.xml
```

## 5. 推荐执行顺序

建议按下面顺序执行，分成“源工作区迁出”和“新工作区回拉验证”两段。

### 5.1 在源工作区生成私有 manifest

在放有原始 manifest 的目录执行：

```bash
python3 fix.py
```

执行后会生成 `final_manifest.xml`。这一步的意义是：

1. 把原来指向上游 tag/commit 的 manifest，转换成统一指向私服分支 `aosp13` 的 manifest。
2. 把不适合直接作为 Gitea 仓库名的 `/` 风格项目名，改成 `_` 风格仓库名。
3. 给无 `path` 的项目补齐实际路径，避免后续扫描和同步时找不到目录。

### 5.2 发布 manifest 仓库

`sync.sh` 并不会从本地 `final_manifest.xml` 读取，而是直接访问远端：

```text
http://10.161.60.141:3000/aosp13/manifests
```

因此在执行 `sync.sh` 前，需要先把当前这套 manifest 文件发布到远端 `manifests` 仓库，至少满足：

1. 仓库名为 `manifests`。
2. 分支为 `master`。
3. 仓库内包含 `final_manifest.xml`。

### 5.3 在源工作区扫描项目映射

这一步必须在 AOSP 源码根目录执行，而不是随便找一个空目录执行，因为脚本会按 manifest 中的 `path` 去找真实目录。

执行：

```bash
./start_repo.sh scan
```

这一步会生成 `project_map.json`，内容是：

1. key 为转换后的仓库名，例如 `device_amlogic_yukawa`。
2. value 为本地目录路径，例如 `device/amlogic/yukawa`。

这个映射文件是后续 `check` 和 `push` 的输入。

### 5.4 在源工作区检查目录和 Git 状态

执行：

```bash
./start_repo.sh check
```

这一步会逐个检查：

1. 目录是否存在。
2. 对应目录下是否存在 `.git`。

如果这里大量报目录不存在，说明当前执行目录不是源码根目录，或者 `XML_FILE` 指向的 manifest 不对。

### 5.5 批量创建并推送子仓库

确认工作区可用于迁移后再执行：

```bash
./start_repo.sh push
```

`push` 模式实际会对每个项目做下面这些动作：

1. 删除当前目录下的 `.git`。
2. 删除所有子目录里的 `.git`。
3. 删除所有 `.gitignore`。
4. 为所有空目录补 `.gitkeep`，确保目录结构可以被 Git 提交。
5. 如果目录完全为空，则自动生成 `README.md`。
6. 重新 `git init`。
7. 提交一份新的初始化提交，提交信息是 `初始化完整目录结构（含空目录）`。
8. 创建并切换到分支 `aosp13`。
9. 调用 Gitea API 自动创建远端仓库。
10. 添加 `origin`。
11. 强制推送 `main` 和 `aosp13` 两个分支。

这里需要注意：

1. manifest 最终使用的是 `aosp13` 分支。
2. `main` 分支只是脚本顺手一起推上去，方便仓库初始化后具备默认分支。
3. 这是一次“重建 Git 历史”的迁移，不会保留原有提交历史。

### 5.6 在新工作区重新初始化并同步

当所有子仓和 `manifests` 仓库都已就绪后，在一个新的干净目录执行：

```bash
./sync.sh
```

`sync.sh` 的行为是：

1. 删除当前目录下的 `.repo`。
2. 用本地 `./repo` 先执行 `repo init`。
3. manifest 地址固定为 `http://10.161.60.141:3000/aosp13/manifests`。
4. manifest 分支固定为 `master`。
5. manifest 文件固定为 `final_manifest.xml`。
6. `repo` 工具本身从清华镜像 `https://mirrors.tuna.tsinghua.edu.cn/git/git-repo` 拉取。
7. 初始化完成后，切换为 `./.repo/repo/repo` 执行：

```bash
./.repo/repo/repo sync --force-sync --fail-fast -j88
```

如果这一步能正常同步完成，说明 manifest、仓库命名和分支映射是通的。

## 6. 一套可执行的落地流程

如果按最稳妥方式操作，可以直接用下面的顺序：

```bash
# 1. 生成新的 manifest
python3 fix.py

# 2. 把 final_manifest.xml 提交到 Gitea 的 manifests 仓库
#    这一步按你自己的仓库管理方式处理

# 3. 在原始源码根目录生成项目映射
./start_repo.sh scan

# 4. 检查目录和 .git 情况
./start_repo.sh check

# 5. 批量创建并推送 1168 个子仓
./start_repo.sh push

# 6. 在新目录验证 repo 回拉
./sync.sh
```

## 7. 迁移完成后的验收点

迁移后至少确认以下几点：

1. Gitea `aosp13` 组织下已经批量生成全部目标仓库。
2. 每个仓库都至少有 `main` 和 `aosp13` 两个分支。
3. `aosp13/manifests` 仓库的 `master` 分支中存在 `final_manifest.xml`。
4. `repo init -u http://10.161.60.141:3000/aosp13/manifests -b master -m final_manifest.xml` 可以成功执行。
5. `repo sync --force-sync --fail-fast -j88` 可以完整拉下所有项目。
6. 抽查几个典型路径，例如 `build/make`、`frameworks/base`、`hardware/rockchip/*` 是否能正常拉取。

## 8. 风险和注意事项

这套脚本能完成迁移，但有几个行为必须明确：

1. `start_repo.sh push` 是破坏性操作，会删除每个项目目录内原有的 `.git` 和 `.gitignore`。
2. `git push` 使用的是 `-f`，会强制覆盖远端分支。
3. 这套迁移保留的是目录内容，不保留原始提交历史。
4. `GIT_PASS` 目前是明文方式参与 HTTP 推送，建议实际使用时改成 token，并避免特殊字符带来的 URL 编码问题。
5. `sync.sh` 默认会先删 `.repo`，所以只能在确认目录可清理时执行。
6. 当前 `fix.py` 只处理了 `remote`、`default`、`project`，没有处理 `superproject`；`final_manifest.xml` 里仍保留了原始 `superproject` 配置。如果目标端没有对应仓库或分支，后续可根据实际情况再决定是否删除该节点。

## 9. 结论

这套代码对应的迁移思路，不是“保留原 Git 历史镜像迁移”，而是：

1. 先把原始 manifest 改造成适合私有 Gitea 的分支和命名规则。
2. 再把现有源码目录重新初始化成一批新的 Git 仓库。
3. 最后用新的 manifest 从私服重新 `repo sync`，验证迁移闭环。

如果后续还要补“批量创建远端分支”或“统一 Git remote alias 为 origin”的流程，需要在现有脚本基础上继续补 manifest remote alias 或 repo forall 推送逻辑；当前仓库里的脚本还没有覆盖这一步。
